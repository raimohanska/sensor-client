{
  "name": "sensor-client",
  "version": "1.0.0",
  "description": "client for sensor-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Juha Paananen",
  "license": "MIT",
  "dependencies": {
    "request-promise": "^1.0.1"
  }
}
